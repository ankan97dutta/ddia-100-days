# Starter: Day 041

Build a tiny leader/follower replication log. `--break` simulates follower lagging by one entry.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
