# Starter: Day 010

Model correlated vs independent failures.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break az-a
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
