# Starter: Day 071

Show how timing assumptions change what you can guarantee. Compare `--model sync` vs `--model async` after a long pause.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --model
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
