# Starter: Day 088

Batch-build a serving-oriented derived dataset. Build versioned user profiles and promote only validated snapshots.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
