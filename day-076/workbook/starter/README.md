# Starter: Day 076

State assumptions explicitly; test designs against them. See which safety claims hold or break under different system-model assumptions.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
