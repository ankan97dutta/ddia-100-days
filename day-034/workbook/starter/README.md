# Starter: Day 034

Trace parse -> plan -> execute -> return. Compare operator choice and scanned row counts.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --no-index
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
