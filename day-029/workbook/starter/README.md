# Starter: Day 029

Work with page-oriented trees and observe access patterns. Compare split counts for sequential vs random key order.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --random
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
