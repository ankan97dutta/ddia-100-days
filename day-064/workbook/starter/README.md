# Starter: Day 064

Show predicate anomalies and what stronger isolation changes. Phantom appears as count increase between reads in one transaction.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
