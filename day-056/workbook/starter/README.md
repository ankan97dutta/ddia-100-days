# Starter: Day 056

Route with metadata and handle stale routing info. Fixes stale cache after move.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --refresh-after-stale
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
