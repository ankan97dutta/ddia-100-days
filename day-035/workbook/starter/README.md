# Starter: Day 035

Compare text vs binary encodings for size, speed, and evolution. Compare bytes per record for JSON vs binary.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
