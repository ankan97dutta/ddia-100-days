# Starter: Day 092

Window by event time; handle late data. Join event streams with watermarks and late-event policies.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
