# Starter: Day 094

Fan one event log out into multiple derived views. Fan order events into analytics and notification views independently.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
