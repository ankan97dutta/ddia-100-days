# Starter: Day 067

Toy coordinator + participants; then kill the coordinator. Shows prepared-but-blocked participants.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --crash
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
