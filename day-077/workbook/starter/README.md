# Starter: Day 077

Build operation histories; spot linearizable vs not. Classify sample register histories as linearizable or not.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
