# Starter: Day 002

Separate transactional and analytical work and see why storage/access patterns diverge. Classifies sample operations and simulates OLAP-on-OLTP contention.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
