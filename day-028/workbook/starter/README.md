# Starter: Day 028

Measure how compaction strategy affects space and reads. Compare `read_fanout` before and after.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
python3 main.py --compact
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
