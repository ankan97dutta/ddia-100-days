# Starter: Day 036

Evolve a schema while old readers/writers keep working. Prints a 2×2 compatibility matrix for v1/v2.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
