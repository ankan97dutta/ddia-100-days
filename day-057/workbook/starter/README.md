# Starter: Day 057

Compare local vs global secondary indexes when data is sharded. Walks an email update on both designs.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --change-email
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
