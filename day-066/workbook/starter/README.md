# Starter: Day 066

Watch lock acquisition and deadlocks. Without `--ordered`, opposite lock order may hang until timeout.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --ordered
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
