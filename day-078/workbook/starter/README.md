# Starter: Day 078

Compare coordination cost with weaker guarantees. Compare simulated write latency for linearizable vs eventual models.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
