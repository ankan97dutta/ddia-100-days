# Starter: Day 061

Watch concurrent snapshots and visibility rules. Triggers write-write detection.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --conflict
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
