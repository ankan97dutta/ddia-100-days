# Starter: Day 096

Compose log, storage, indexes, and serving on purpose. Print component ownership and what stays stable when swapping search.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
