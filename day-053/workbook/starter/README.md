# Starter: Day 053

Partition by key range; feel hot ranges. To simulate splitting the hot trailing range.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --split-at 9000
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
