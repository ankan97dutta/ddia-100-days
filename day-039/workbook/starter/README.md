# Starter: Day 039

Reason about producer/consumer deploy order. Prints safe/unsafe at each deploy step.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break --change remove_field
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
