# Starter: Day 048

Implement deterministic conflict resolution (and know its limits). LWW on sets drops tags; union merge preserves them.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break --skew
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
