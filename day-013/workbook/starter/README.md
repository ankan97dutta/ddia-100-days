# Starter: Day 013

Design for debugging, deploys, rollbacks, and boring weekday ops.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --have deploy --have logs
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
