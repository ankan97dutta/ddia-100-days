# Starter: Day 091

Capture DB changes into a derived view. Apply a change log and verify the projection converges.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
