# Starter: Day 059

Make A/C/I/D concrete with failure stories. Shows atomic rollback on partial transfer.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --fail-mid
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
