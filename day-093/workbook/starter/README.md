# Starter: Day 093

Restart a processor without corrupting derived state. Checkpoint stream state and compare good vs mismatched recovery.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
