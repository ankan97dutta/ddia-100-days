# Starter: Day 070

Show that a timeout is a guess, not proof of failure. Lowers timeout to inflate ambiguous outcomes.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --aggressive
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
