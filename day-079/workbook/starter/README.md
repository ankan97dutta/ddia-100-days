# Starter: Day 079

Implement Lamport timestamps. Simulate Lamport clocks across three processes.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
