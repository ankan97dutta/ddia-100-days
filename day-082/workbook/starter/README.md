# Starter: Day 082

Get concrete about agreement, validity, and termination. Simulate majority voting with competing proposals and node loss.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
