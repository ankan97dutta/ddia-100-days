# Starter: Day 080

Use logical time to order concurrent events. Compare vector clocks and detect causal vs concurrent events.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
