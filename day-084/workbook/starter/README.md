# Starter: Day 084

Build a log-analysis pipeline with boring tools. Parse sample logs and verify deterministic batch output.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
