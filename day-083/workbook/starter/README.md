# Starter: Day 083

Decide where coordination belongs, and where it is waste. Score whether coordination belongs on each architecture component.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
