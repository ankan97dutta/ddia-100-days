# Starter: Day 072

Simulate skew and backwards wall-clock jumps. Simulates backward wall time.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --ntp-step-back-ms 500
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
