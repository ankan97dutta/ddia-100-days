# Starter: Day 081

Design an ID service and name its consistency needs. Compare DB sequences, UUIDs, and Snowflake-style IDs.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
