# Starter: Day 031

Build and compare a secondary-index approach. Compare `write_touches` with and without the secondary index.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --no-index
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
