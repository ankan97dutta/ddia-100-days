# Starter: Day 040

Design evolution for APIs and events across services. Shows consumer/producer version mismatch on required fields.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
