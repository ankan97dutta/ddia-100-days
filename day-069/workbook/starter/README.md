# Starter: Day 069

Inject latency, drops, duplicates, and reordering. Compare runs with and without `--idempotent`.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --idempotent
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
