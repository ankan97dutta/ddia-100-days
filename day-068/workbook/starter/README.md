# Starter: Day 068

Build something where pieces fail independently. Demonstrates the dual-write bug.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --fail-b --assume-success
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
