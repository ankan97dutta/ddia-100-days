# Starter: Day 095

Same output via batch recompute and incremental stream. Compare batch and streaming DAU and detect drift.

## Run

```bash
python3 main.py --help
python3 main.py
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
