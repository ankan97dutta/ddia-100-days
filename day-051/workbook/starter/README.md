# Starter: Day 051

Decide when a single primary is no longer enough. `python3 main.py --bad-shard-share 0.9` Adjust growth and limits to model your workload.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --bad-shard-share 0.9
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
