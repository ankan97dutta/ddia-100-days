# Starter: Day 005

Find where distribution helps and where it mostly adds failure modes. Lists failure modes for single-node vs distributed designs.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
