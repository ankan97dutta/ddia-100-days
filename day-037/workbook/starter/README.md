# Starter: Day 037

Version messages and test compatibility with field numbers. Simulates proto field tags without the protobuf package.

## Run

```bash
python3 main.py --help
python3 main.py
python3 main.py --break
```

Requires Python 3.10+ (stdlib only). Creates a `data/` folder next to `main.py` when needed.
